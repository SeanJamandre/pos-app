# Database Note

This first version of the POS system does not use a database. Customer and user records are static PHP arrays used as temporary data sources, as required by the laboratory activity.

